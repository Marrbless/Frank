# Role label for desktop or lab runs.
PBOT_ROLE=lab

# Allow local process execution in developer-controlled environments.
PBOT_ALLOW_EXEC=1

# Allow network access in developer-controlled environments.
PBOT_ALLOW_NETWORK=1

# Approval posture expected for developer-controlled runs.
PBOT_APPROVAL_MODE=developer
